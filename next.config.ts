import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Server-side rendering for API routes to work
  // Static export removed - using SSR for live data
};

export default nextConfig;
